name: Auto formatting and linting

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # Web formatting check
  Prettier_Format_For_Web:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    # - name: Set up Node.js for formatting & linting
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: '18'

    # - name: Install Prettier
    #   run: npm install --no-save prettier@2.8.8

    # - name: Check formatting with Prettier
    #   run: npx prettier --check "website/**/*.{html,css,js,md}"